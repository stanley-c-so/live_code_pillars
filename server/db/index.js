const db = require('./database');
const Trainer = require('../db/models/trainer');
const Pokemon = require('../db/models/pokemon');

//Associations
// Define a one-to-many relationship between Trainer and Pokemon

module.exports = {
  // Trainer,
  // Pokemon,
  // db,
};
