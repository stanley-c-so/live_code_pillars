'use strict';

const db = require('../database');
const Sequelize = require('sequelize');

// Model
// Create a Trainer model with a name and an array of badges they've won



// Method
// Create a method that returns the number of badges a particular trainer has.
