const router = require('express').Router();
const { Trainer, Pokemon } = require('../db/index');

//Routes

//Retrieve all Trainers from the database.

//Retrieve a single Trainer from the database.

//Given a Trainer's ID number, update the trainer's name.
//This route should only make *one* call to the dataabase!

module.exports = router;
